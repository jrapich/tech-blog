<section class="row postrow" id="{{id}}">
    <div class="col">
        {{post_title}}
    </div>
    <div class="col-4">
        {{user.username}}
    </div>
    <div class="col-2">
        {{created_on}}
    </div>
</section>